#!/usr/bin/env groovy
pipeline {
    agent any
    environment {
        AWS_ACCESS_KEY_ID = "AKIAT2XFSMJR6O5OLWVS"
        AWS_SECRET_ACCESS_KEY = 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""
        AWS_DEFAULT_REGION = "us-east-1"
    }
    stages {
        stage("Create an EKS Cluster") {
            steps {
                script {
           
                        bash "terraform init"
                        bash "terraform apply -auto-approve"
        
                }
            }
        }
        stage("Deploy to EKS") {
            steps {
                script {
         
                        bash "aws eks update-kubeconfig --name myapp-eks-cluster"
                        bash "kubectl apply -f nginx-deployment.yaml"
                        bash "kubectl apply -f nginx-service.yaml"
         
                }
            }
        }
    }
}
